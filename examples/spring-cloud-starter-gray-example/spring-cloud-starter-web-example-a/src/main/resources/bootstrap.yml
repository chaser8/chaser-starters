chaser:
  nacos:
    server-addr: nacos.yangzb.com:18848
    username: develop
    password: $2a$10$ptWtT1bLFsHkw/NwYwMpIOnYZKhFj4QURVkq59wKGJfcz.wQhl8Li
    namespace: test
  swagger:
    enable: true
server:
  port: 9003
spring:
  devtools:
    restart:
      enabled: false
  application:
    name: nacos-provider-a
  cloud:
    nacos:
      server-addr: ${chaser.nacos.server-addr}
#      username: ${chaser.nacos.username}
#      password: ${chaser.nacos.password}
      namespace: ${chaser.nacos.namespace}
      discovery:
        group: test
        metadata:
          version: 1.0
      config:
        namespace: ${chaser.nacos.namespace}
        extension-configs:
          - data-id: gray.yaml
            group: test
            refresh: true
management:
  endpoints:
    web:
      exposure:
        include: '*'
logging:
  level:
    root: info
    top.chaser: trace